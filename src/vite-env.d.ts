/// <reference types="vite/client" />

// Extend or document environment variables here if you need custom ones.
// Vite already exposes `import.meta.env` typings via the `vite/client` types above.

export { }
